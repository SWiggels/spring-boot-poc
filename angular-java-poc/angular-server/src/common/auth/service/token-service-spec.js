describe('tokenService', function() { 

  beforeEach(module('appConfig','auth'));
  //beforeEach(module('configService'));
  it('should RUN.', inject(function(tokenService) {

	//expect(tokenService.doSomething()).toEqual('something');

  }));

});